/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Permite algumas configurações mais flexíveis, caso precisemos de Server Actions
  },
};
export default nextConfig;
